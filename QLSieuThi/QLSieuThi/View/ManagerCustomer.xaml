<Window x:Class="QLSieuThi.View.ManagerCustomer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QLSieuThi.View"
        mc:Ignorable="d"
        Title="Manage Customers" Height="700" Width="1000">

    <Grid Background="#E3F2FD">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <StackPanel Grid.Row="0" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,20">
            <TextBlock Text="ManageShop" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,10" Foreground="#1565C0"/>
            <TextBlock Text="Customer Management" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,10" Foreground="#1565C0"/>
            <Button x:Name="btn_Back" Content="Trở lại" Margin="10" Click="Btn_Back_Click"/>
        </StackPanel>

        <!-- Tìm kiếm khách hàng -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,5,5,5">
            <TextBlock Text="Search by Customer Name:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16" Foreground="#0D47A1"/>
            <TextBox Name="searchTextBox" Width="300" Margin="0,0,20,0" FontSize="16" Padding="5" BorderBrush="#0D47A1" TextChanged="SearchTextBox_TextChanged" />
        </StackPanel>

        <!-- Danh sách khách hàng -->
        <DataGrid Grid.Row="2"
          Margin="5"
          Name="customersDataGrid"
          Background="White"
          BorderBrush="#1565C0"
          BorderThickness="2"
          AutoGenerateColumns="False"
          CanUserAddRows="False" Loaded="CustomersDataGrid_Loaded">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Người Dùng" Binding="{Binding MaND}" Width="200"/>
                <DataGridTextColumn Header="Họ và Tên" Binding="{Binding HoTen}" Width="200"/>
                <DataGridTextColumn Header="CCCD" Binding="{Binding CCCD }" Width="150"/>
                <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SoDT}" Width="150"/>
                <DataGridTextColumn Header="Mã Hóa Đơn" Binding="{Binding MaHD}" Width="150"/>
                <DataGridTextColumn Header="Vai trò" Binding="{Binding MaVaiTro}" Width="150"/>
                
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
